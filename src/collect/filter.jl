function _collect(src, q::Node{:filter})

    [ srcs[token] for token in index ]

    indices = tuple([  ])

    for h in q.helpers

    end

    for (field, T) in zip(flds, map(eltype, eltypes(src, flds...)))
        res[field] = NullableVector{T}()
    end

    for i in rows
        for p in preds
            if p(subset)
                push!()
    end
end
